{{- if .Values.mysql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: mysql
  labels:
    app: mysql
spec:
  # StatefulSet과 반드시 짝으로 가는 값
  clusterIP: None  # Headless Service

  selector:
    app: mysql

  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
{{- end }}
